version: '3'
services:
  goldtalk_api:
    build: .
    image: goldtalk_img:${TAG}
    container_name: goldtalkAPI
    restart: always
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=prod
    volumes:
      - /tmp/aaa:/app/output/log